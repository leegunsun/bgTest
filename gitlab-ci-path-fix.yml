# Quick path fix alternative - replace lines 216-225 with:

    # 경로 확인 및 배포 실행
    - ssh "ubuntu@$DEPLOY_SERVER_DEV" 'bash -lc "
        cd ~/bgTest/v5ToWindow && 
        echo \"Checking Docker installation paths...\" &&
        which docker || echo \"Docker not in PATH\" &&
        which docker-compose || echo \"Docker Compose not in PATH\" &&
        ls -la /usr/bin/docker* /usr/local/bin/docker* 2>/dev/null || true &&
        echo \"Stopping existing containers...\" &&
        /usr/bin/docker-compose down || /usr/local/bin/docker-compose down || sudo docker-compose down || true &&
        echo \"Rebuilding Docker image...\" &&
        /usr/bin/docker build -t blue-green-nginx . || /usr/local/bin/docker build -t blue-green-nginx . || sudo docker build -t blue-green-nginx . &&
        echo \"Starting updated containers...\" &&
        /usr/bin/docker-compose up -d || /usr/local/bin/docker-compose up -d || sudo docker-compose up -d &&
        echo \"✅ Deployment completed\"
      "'